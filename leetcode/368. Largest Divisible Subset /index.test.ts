import { largestDivisibleSubset } from './index'

describe('最大整除子集', () => {
  it('test', () => {
    // expect(largestDivisibleSubset([1, 2, 3])).toEqual([1, 2])
    // expect(largestDivisibleSubset([1, 2, 4, 8])).toEqual([1, 2, 4, 8])
  })
})
